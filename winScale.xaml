<Window x:Class="wpfTDX.winScale"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:wpfTDX"
        mc:Ignorable="d"
        Title="position scaling" Height="200" Width="800" ResizeMode="NoResize">
    <StackPanel>
        <!-- SharedSizeScope ensures both label columns have the same width -->
        <Grid Grid.IsSharedSizeScope="True">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <!-- fields -->
                <RowDefinition Height="Auto"/>
                <!-- spanning button -->
            </Grid.RowDefinitions>

            <!-- LEFT column (names preserved) -->
            <Grid Grid.Column="0" Grid.Row="0" Margin="10 8 10 0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" SharedSizeGroup="LabelCol"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="lblFundGroupName" Grid.Row="0" Grid.Column="0"
                           Text="fundgroupname:" FontWeight="Bold"
                           VerticalAlignment="Center" Margin="0 0 8 6"/>
                <TextBox x:Name="txtFundGroupName" Grid.Row="0" Grid.Column="1"
                         IsReadOnly="True" Margin="0 0 0 6"/>

                <TextBlock x:Name="lblSubaccount" Grid.Row="1" Grid.Column="0"
                           Text="fundname:" FontWeight="Bold"
                           VerticalAlignment="Center" Margin="0 0 8 6"/>
                <TextBox x:Name="txtSubaccount" Grid.Row="1" Grid.Column="1"
                         IsReadOnly="True" Margin="0 0 0 6"/>

                <TextBlock Grid.Row="2" Grid.Column="0"
                           Text="tickername:" FontWeight="Bold"
                           VerticalAlignment="Center" Margin="0 0 8 0"/>
                <TextBox x:Name="txtTickername" Grid.Row="2" Grid.Column="1"
                         IsReadOnly="True"/>
            </Grid>

            <!-- RIGHT column (names preserved) -->
            <Grid Grid.Column="1" Grid.Row="0" Margin="10 8 10 0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" SharedSizeGroup="LabelCol"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0"
                           Text="step size:" FontWeight="Bold"
                           VerticalAlignment="Center" Margin="0 0 8 6"/>
                <TextBox x:Name="txtStepSize" Grid.Row="0" Grid.Column="1"
                         Text="0.1" Margin="0 0 0 6"/>

                <TextBlock x:Name="lblScaledPosition" Grid.Row="1" Grid.Column="0"
                           Text="scaled position:" FontWeight="Bold"
                           VerticalAlignment="Center" Margin="0 0 8 6"/>
                <TextBox x:Name="txtScaledPosition" Grid.Row="1" Grid.Column="1"
                         IsReadOnly="True" Margin="0 0 0 6"/>

                <TextBlock Grid.Row="2" Grid.Column="0"
                           Text="scaled target:" FontWeight="Bold"
                           VerticalAlignment="Center" Margin="0 0 8 0"/>
                <TextBox x:Name="txtScaledTarget" Grid.Row="2" Grid.Column="1"/>
            </Grid>

            <!-- Spanning button -->
            <Button Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                    Content="update" FontWeight="Bold"
                    Margin="10" Height="32"
                    HorizontalAlignment="Stretch"
                    Click="Button_Click"/>
        </Grid>
    </StackPanel>
</Window>
