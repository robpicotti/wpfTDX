<UserControl x:Class="wpfTDX.ucTickerFreezer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:wpfTDX"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="2">
        <Border BorderBrush="Black" BorderThickness="1" Padding="5" Margin="5">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <Button 
                        x:Name="cmdRefresh" Content="Refresh" Click="Button_Click" Width="100" Margin="5"
                        HorizontalAlignment="Left"
                     />
                    <DataGrid x:Name="dgTickerFreezer" AutoGenerateColumns="True"
                                Height="500"       
                                ScrollViewer.CanContentScroll="True" 
                                ScrollViewer.VerticalScrollBarVisibility="Auto"
                                ScrollViewer.HorizontalScrollBarVisibility="Auto"
                              MouseRightButtonDown="dgTickerFreezer_MouseRightButtonDown"
                                Margin="5">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="FontWeight" Value="Bold" />
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="thaw tad_id?" Click="MenuItem_Click"/>
                                <MenuItem Header="override tad_id?" Click="MenuItem_Click_1">
                                    <MenuItem Header="expire at close?" Click="MenuItem_Click_2"/>
                                    <MenuItem Header="no expiration?" Click="MenuItem_Click_3"/>
                                </MenuItem>
                                <MenuItem Header="remove override for this tad_id?" Click="MenuItem_Click_4"/>
                            </ContextMenu>
                        </DataGrid.ContextMenu>
                    </DataGrid>
                </StackPanel>
                <StackPanel Grid.Column="1">
                    <TextBlock 
                        x:Name="cmdClose" Text="X" MouseLeftButtonDown="cmdClose_MouseLeftButtonDown" Width="100" 
                        HorizontalAlignment="Right"
                        FontWeight="Bold"
                        Margin="1 5 -80 0"
                     />
                </StackPanel>
            </Grid>
        </Border>
    </StackPanel>
</UserControl>
