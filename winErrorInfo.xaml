<Window x:Class="wpfTDX.winErrorInfo"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:wpfTDX"
        mc:Ignorable="d"
        Title="Error Info" Height="500" Width="700" ResizeMode="CanResize" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
            <TextBlock Text="Error Code:" FontWeight="Bold" Width="100" VerticalAlignment="Center"/>
            <TextBlock x:Name="txtErrorCode" FontSize="14" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Error:" FontWeight="Bold" Width="100" VerticalAlignment="Top"/>
            <TextBlock x:Name="txtErrorString" FontSize="14" TextWrapping="Wrap" MaxWidth="540"/>
        </StackPanel>

        <TextBlock Grid.Row="2" Text="Documentation (how to investigate / fix this error):"
                   FontWeight="Bold" Margin="0,0,0,5"/>

        <TextBox Grid.Row="3" x:Name="txtDocumentation"
                 AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 IsReadOnly="True"
                 FontFamily="Consolas" FontSize="13"
                 Padding="5"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="cmdEdit"   Content="Edit"   Width="80" Margin="5" Click="cmdEdit_Click"/>
            <Button x:Name="cmdSave"   Content="Save"   Width="80" Margin="5" Click="cmdSave_Click" IsEnabled="False"/>
            <Button x:Name="cmdCancel" Content="Cancel" Width="80" Margin="5" Click="cmdCancel_Click" IsEnabled="False"/>
            <Button x:Name="cmdClose"  Content="Close"  Width="80" Margin="5" Click="cmdClose_Click"/>
        </StackPanel>
    </Grid>
</Window>
